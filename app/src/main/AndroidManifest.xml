<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.haniftaqiuddin.almatshurat" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >

        <activity android:name=".Splash"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".MainActivity"
            android:label="@string/app_name" />

        <activity
            android:name=".DzikirPagi"
            android:label="@string/title_activity_dzikir_pagi" />

        <activity
            android:name=".DzikirPetang"
            android:label="@string/title_activity_dzikir_petang" />
/
        <activity
            android:name=".Keutamaan"
            android:label="@string/title_activity_keutamaan" />


    </application>

</manifest>
